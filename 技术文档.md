# VideoTime（时间统计）技术文档

> 适用程序：**VideoTime**（窗体标题「时间统计」）
> 技术栈：C# + WinForms，目标 **.NET Framework 4.8**
> 本文面向普通用户为主，兼顾开发者参考。

---

## 目录

1. [软件概述](#一软件概述)
2. [快速上手 · 使用方法](#二快速上手使用方法)
3. [详细功能清单](#三详细功能清单)
4. [程序结构](#四程序结构)
5. [关键技术实现要点](#五关键技术实现要点)
6. [构建方式与环境要求](#六构建方式与环境要求)
7. [常见问题 FAQ](#七常见问题-faq)
8. [未来规划与候选功能](#八未来规划与候选功能)

---

## 一、软件概述

**VideoTime（时间统计）** 是一款 Windows 桌面小工具，用于**批量统计一个或多个文件夹（含其子文件夹）里所有视频（mp4/mov/m4v/3gp、mkv/webm、avi、wmv/asf 等）的总时长**，并以**树形列表**直观展示每个文件夹各自累计的时长与视频数量。同时支持**命令行批量统计**与 **CSV/HTML 报表导出**。

它解决的核心痛点：课程/资料文件夹里有成百上千个分散的视频，人工一个个看属性统计总时长非常繁琐。本软件**直接读取视频文件的二进制时间头**（不解码视频），因此上 TB、上千文件的目录也能在数秒内完成统计。

主要能力一览：

- 选择文件夹：**多行输入框**，可同时统计多个文件夹（每行输入 / 浏览 / 拖放，`+` 加行、`-` 删行）
- 可选是否递归统计子文件夹
- 一键查询，后台多线程扫描，界面不卡顿；支持随时取消
- 树形展示「文件夹名 + 总时长 + 视频数量」，默认展开到第 3 层
- **自绘进度条**（`ProgressBarEx`，进度文字内嵌，收集阶段滑动动画、解析阶段百分比）与**取消按钮**
- **结果筛选**：按名称 / 时长 / 视频数过滤结果树，命中文字高亮，枝干累计只统计过滤后的可见叶节点
- 在树行内**直接拖选部分文字**并复制
- **复制**（Ctrl+C / 右键）与**保存为图片**（PNG/BMP）导出结果
- **导出报表**（CSV / HTML，含失败明细）
- **命令行模式**（`-d / -r / -o`，无界面批量统计）
- **设置**：日志输出级别可调
- 多路径、选项与日志级别记忆（下次启动自动回填）

---

## 二、快速上手（使用方法）

### 2.1 启动程序

双击 `VideoTime.exe` 即可打开主窗口（默认 420×570、居中显示、标题「时间统计」）。

### 2.2 选择要统计的文件夹

主窗口顶部为**「文件」标签页**，默认显示一行文件夹输入框，每行右侧有 `[浏览]` 与 `[-]` 按钮，标题栏右侧有 `[+]` 加行按钮。每行提供三种方式，任选其一：

- **① 直接输入**：在输入框里输入或粘贴目录路径，例如：
  `E:\课程视频\张宇2027考研数学`
- **② 浏览选择**：点行内 `[浏览]` 按钮，在弹出的系统文件夹对话框中选择目标文件夹，路径会自动填入。
- **③ 拖放**：从资源管理器把一个文件夹**直接拖到**输入框上（鼠标会显示「复制」标记），松开即自动填入。拖入的是**文件**则不会生效。

多文件夹统计：

- 点右上角 `[+]` 添加新输入行，可输入/浏览/拖放第二个、第三个文件夹；`[-]` 删除某行。
- 有内容（非空）时 `[+]` 才可继续加行；某行失去焦点时若已为空会自动删除（至少保留一行）。
- 同时统计多个文件夹时，各根目录并行收集与解析，结果合并到同一棵树（同名根目录以完整路径区分显示），并**检测重叠**：若存在重复路径或嵌套包含，会弹出提示并询问是否允许重复计时。
- 存在无效路径时（多路径情况下）会列出无效路径并询问「忽略并继续扫描其余路径」或取消。

### 2.3 选择是否包含子文件夹

- 勾选 **☑ 包含子文件夹**：统计所选文件夹以及它下**所有层级**的子文件夹。
- 不勾选：**只看该文件夹直接**包含的视频（不含子目录）。

> 该勾选会记忆，下次启动保持上次的选择。

### 2.4 开始查询

点击 **[查询]** 按钮。此时：

- 按钮会暂时变为不可用、鼠标变为"沙"形，顶部显示「正在扫描文件夹…」。
- **进度条**开始工作：窗口底部的自绘进度条（`ProgressBarEx`）先进入**收集目录**阶段（进度条为滑动动画，内置文字「正在收集目录…」），随后进入**解析**阶段，显示 **百分比 + 已处理/总数**，如「正在读取 45% (523/1159)」。
- 扫描期间可随时点击 **[取消]** 按钮中止（顶部显示「扫描已取消。」，未完成部分不写入结果）。
- 扫描完成（通常几秒）后顶部显示结果，例如：
  `总时间: 1234时56分34秒`
- 若存在缺失（文件读取失败 / 目录无法访问 / 超深目录省略），顶部**仍只显示总时间**，扫描结束后会**弹出提示窗**汇总缺失情况，详细原因写入 `log.txt`（详见「二.13 缺失提示」）。

### 2.5 阅读结果树

扫描完成后（结果主体）会以树形呈现，**每行 = 文件夹名 + 总时长 + 视频个数**：

```
📁 张宇2027考研数学  1234时56分34秒  [视频401]
   └ 01.基础阶段  153时23分0秒  [视频221]
   └ 02.强化阶段   89时11分12秒  [视频177]
```

- **时长**为该文件夹及其所有子文件夹的**累计时长**；`[视频N]` 中的 N 是该文件夹（含子级）内**按扩展名检索到的视频文件总数（含读取失败者）**。注意：总时长只累加**成功解析**的文件；读不到时长的视频仍计入数量 N，但其时长计入 0。故当目录存在"读取失败"文件时，`[视频N]` 会略大于"实际计入时长的文件数"（失败文件需在失败明细/`log.txt` 中另行查看）。
- 展开的列表**已自动展开到第 3 层（含根目录）**，更深的层级可点行左侧的 `+ / -` 手动展开或收起。

### 2.6 结果筛选

主窗口顶部的**「筛选」标签页**（点菜单 `筛选` 切换）可在扫描完成后对结果树做二次过滤，所有条件输入框内按 `Enter` 或点 `[过滤]` 立即生效：

- **名称**：包含关键字，不区分大小写。
- **时长（小时）**：最小 / 最大累计时长区间，仅作用于**叶子文件夹**（无子文件夹的最底层）。
- **数量**：最小 / 最大视频数区间，仅作用于叶子文件夹。
- 点 `[过滤]` 应用：只保留命中的叶子节点及其祖先枝干，枝干文字重算为过滤后可见叶子的累计时长/数量，顶部显示 `过滤合计: X时Y分Z秒 [视频N]`；名称命中处**蓝色高亮**。
- 点 `[清除]` 还原：恢复过滤前全部节点、原始文字与展开状态，顶部回到 `总时间: …`。

### 2.7 复制结果

- **复制单个文件夹行**：单击选中该行后按 `Ctrl+C`（或右键 → 复制）。
- **复制当前界面所有可见文本**：右键 → 「复制当前界面文本」。
- **拖选再复制局部**：在某个节点文字上按住左键横向拖动，蓝色高亮选中部分文字，松开后按 `Ctrl+C` 或右键 → 复制，即只复制选中的那一段（例如只复制时长数字）。

### 2.8 保存为图片

右键 → 「保存当前界面文本为图片」，在弹出的保存对话框中选 PNG 或 BMP 并指定位置保存。图片为**高清（3× 倍率、288dpi）**的白色黑字文本图，适合发朋友圈 / 做汇报截图；结果行数超过 30000 行时按像素预算自动等比缩放以防位图内存无界。

### 2.9 日志

程序运行目录下会生成 `log.txt`（UTF-8），每行带时间戳与**等级标签**，按 `错误 / 警告 / 信息` 三级记录（详见「三.G 日志」）。

- 例如 `2026-08-03 10:28:26  [警告] 扫描完成但存在缺失: 43 个文件读取失败；4 个目录无法访问 | E:\ | 耗时: 1,400 毫秒`。
- 日志输出级别可在**设置**窗口中统一选择（详见「二.10 设置」）。

### 2.10 设置（日志输出级别）

主窗口右上角的 **[设置]** 按钮会打开模态设置窗口，其中「日志输出级别」下拉框可整体调节日志的写入范围，共四档：

| 选项 | 含义 | 等价等级 |
|---|---|---|
| 全部（信息 / 警告 / 错误） | 所有日志都写 | `Info` |
| 仅警告及以上 | 只写警告与错误 | `Warning` |
| 仅错误 | 只写错误 | `Error` |
| 关闭日志 | 不写任何日志 | `Off` |

点击「确定」即保存到 `user.config`，立即生效并持续到下次修改。

### 2.11 导出报表（CSV / HTML）

扫描完成后，在结果树上**右键 → 「导出报表…」**，选择保存为 **CSV** 或 **HTML** 文件：

- **CSV**：可用 Excel / WPS 打开，含各文件夹（层级缩进、累计时长、秒数、视频数）与**失败明细区**（类型 / 路径 / 原因）。
- **HTML**：浏览器直接打开的美观表格，同样含总时间、各文件夹明细与失败明细区。
- 默认文件名 `时间统计报表_yyyyMMdd_HHmmss.csv`；导出成功/失败均弹提示并写日志。
- 命令行模式下也可用 `-o 输出文件` 直接生成报表（见「二.12」）。

### 2.12 命令行模式

在 CMD / PowerShell 中直接调用 `VideoTime.exe`（不带参数则照常打开图形界面）：

```
VideoTime.exe -d <文件夹> [-r] [-o <输出文件>] [-h]
```

- `-d, --folder`：要统计的文件夹路径（**必填**）。
- `-r, --recursive`：递归统计子文件夹（默认不递归）。
- `-o, --out`：输出报表文件，`.csv` 或 `.html` 结尾自动判断格式（默认 CSV）；不指定则在控制台打印结果树。
- `-h, --help`：显示帮助。

行为与退出码：

- 扫描完成输出三行摘要：`总时间: …`、`已统计视频: N 个`、有缺失时 `缺失: …`；成功退出码 **0**。
- 目录不存在 / 参数错误：向 stderr 输出错误信息，退出码 **1**。
- 命令行运行同样会向 `log.txt` 记录一条日志（受日志级别设置控制）。

### 2.13 缺失提示（弹窗 / 总时间栏）

扫描结束时，缺失情况统一处理为 **弹窗汇总 + 日志明细**，总时间栏只显示总时间：

- **有缺失**（文件读取失败、目录无法访问、超深目录省略之一）→ 扫描结束后弹出警告窗，列出缺失数量并提示"详细原因已写入日志文件"；`log.txt` 记录每项的**路径与失败原因**（每类最多 200 条，超出则合并省略）。
- **无缺失** → 不弹窗，仅正常显示总时间并写 Info 日志。
- 各类缺失的具体提示方式与日志等级详见「三.10 结果反馈与缺失提示」。如复制、保存图片、导出报表等操作类提示见「三.E / F」，日志见「三.G」。

---

## 3. 详细功能清单

### A. 目录来源（多行输入）

主窗口顶部「文件」标签页内含**动态多行输入区**：首行由设计器生成（`TextBox_Doc` + `BtnBrowse` + `BtnRemoveRow1`），后续行由 `AddInputRow` 运行时创建。每行 = 文本框 + `[浏览]` + `[-]`，标题栏 `[+]` 加行。所有行按当前面板宽度自适应布局（文本框拉伸、按钮右对齐）。

**1. 文本框直接输入**
- 操作：在任一输入框键入/粘贴 `E:\课程视频\张宇2027考研数学` 这类绝对路径。
- 边界：启动时自动回填上次记忆的多条路径（`RootPaths` 以 `;` 分隔），首次为空白；单路径且无效时点「查询」会提示「文件夹路径无效，请重新输入。」，不执行扫描。
- 实现：`Start_Click` 收集每行 `TextBox.Text`，经 `VideoScanner.NormalizePath`（`Trim().Trim('"')` 去空格引号、`/` 归一化为 `\`）后逐个 `Directory.Exists` 校验，区分 `validPaths` / `invalidPaths`。

**2. 浏览按钮**
- 操作：点行内「浏览」→ 系统文件夹对话框 → 选定后路径自动填入该行。
- 实现：`BtnBrowse_Click`（首行）与 `AddInputRow` 内创建的 `btnBrowse.Click` 回调共用 `folderBrowserDialog1`，选中后回填对应文本框。

**3. 拖放文件夹到输入框**
- 操作：从资源管理器拖一个文件夹到任一输入框上（鼠标显示"复制"），松开自动填入路径。
- 边界：**只接受目录**；拖入文件时 `Directory.Exists` 校验失败，不填入。首行已挂 `DragEnter`/`DragDrop`；运行时创建的行由 `AddInputRow` 同样挂接。
- 实现：`DragEnter` 判断数据含 `FileDrop` 并设 `DragDropEffects.Copy`；`DragDrop` 取首个路径，仅当为存在的目录才写入。

**3b. 多行增删与空行回收**
- `[+]` 仅当已存在非空行时可用（`BtnAddRow_Click` 检查 `_inputRows.Any(非空)`），否则忽略。
- `[-]` 删除该行；至少保留一行（`_inputRows.Count <= 1` 时不删）。
- 空行回收：某行文本框失去焦点（`InputRow_Leave`）时若内容为空且非末行，自动删除并重排；`Start_Click` 前还会 `RemoveBlankRows()` 清理全部空行（保留末行）。
- 实现：`AddInputRow` / `BtnRemoveRow_Click` / `InputRow_Leave` / `RemoveBlankRows`，统一经 `ReindexRows()` 按 `RowTopBase + i*RowHeight` 重排并 `AdjustUpperPanelHeight()` 调整面板高度。

**3c. 重叠检测（多根）**
- 功能：多路径提交前两两检测 `DetectOverlaps`，识别**重复路径**与**嵌套包含**（`a.StartsWith(b + "\\")` 双向判断，忽略大小写）。
- 行为：存在重叠时弹提示列出各重叠项，询问「仍继续扫描（允许重复计时）？」；选否则取消（顶栏「扫描已取消。」）。
- 实现：`Start_Click` 中 `validPaths.Count > 1` 时调用 `DetectOverlaps(validPaths, overlapWarnings)`。

**4. 包含子文件夹复选框**
- 说明：位于输入行上方左侧的勾选框，控制是否递归子目录。
- 操作：勾选 = 统计所选各文件夹及**所有层级**子目录；不勾选 = 只统计每个文件夹直接包含的视频。
- 边界：勾选状态会记忆，下次启动保留。
- 实现：`Start_Click` 读取 `CbSubfolders.Checked` 传入 `VideoScanner.RunMultiple` 的 `recursive` 参数。

### B. 扫描与统计（「查询」按钮）

**5. 异步扫描不卡界面**
- 说明：点「查询」后后台线程扫描，界面保持响应，可继续拖动窗口。
- 行为：扫描期间「查询」按钮禁用、光标变"沙"形、顶栏显示「正在扫描文件夹…」；完成后全部恢复。
- 实现：`Start_Click` 内 `await Task.Run(...)` 执行 `VideoScanner.RunMultiple`；`Start.Enabled=false` + `Cursor=WaitCursor`，`finally` 里恢复。

**5b. 多根目录扫描（RunMultiple）**
- 说明：一次查询可对**多个根目录**并行扫描，结果合并为同一棵结果树；单根时退化为 `Run(root, …)`。
- 收集：每个根目录各自 `CollectFoldersRecursive` 生成 `FolderItem[]`，文件数合计为总进度基数。
- 解析：外层 `Parallel.For(0, roots.Length, …)` 按根并行，内层每根用 `DurationParser.ReadAll` 读文件时长；并发度收敛为 `outerThreads = min(roots.Length, max(2, threads/2))`、`innerThreads = max(2, threads/outerThreads)`，避免外层×内层线程数二次方爆炸。
- 合并：`merged` 汇总各根的 `FolderResults`（根同名以完整路径区分显示，见「C.12」）/ `FailedFiles` / `FailedDirs` / `SkippedDirs` / `FailCount` / `DirFail` / `DepthSkipped` / `TotalSeconds`；`TotalFileCount` 为全部分根文件总数。
- 重叠/无效路径处理见「A.3c」与「2.2」。

**6. 多线程并发读取**
- 说明：所有视频文件被并行读取时长，充分利用多核 CPU，文件越多速度优势越明显。
- 实现：`DurationParser.ReadAll` 内 `Parallel.ForEach`，`MaxDegreeOfParallelism` 由调用方传入（单根为 `Math.Max(2, Environment.ProcessorCount)`，多根按「5b」收敛）；按 `files` 顺序写入定长数组 `ordered[]/failFlags[]`，`Interlocked` 标记失败，最终按顺序组装 `Dictionary`，支持 `CancellationToken` 与逐文件进度回调。

**7. 多格式时长直读（核心算法，DurationParser）**
- 说明：**不解码视频**，直接读视频文件二进制头取时长，因此即使几百 GB / 成千文件也能秒级统计。
- 支持的格式（按扩展名白名单过滤，`GetVideoFiles`）：
  - **mp4 / mov / m4v / 3gp**：解析 `moov` 容器中的 `mvhd` 盒。
    自适应窗口：先扫文件**尾部 4MB**，再扫**头部 4MB**，查找 `moov` 特征；仍找不到则扫**尾部 128MB**（或覆盖整个文件）。每个窗口按 **1MB 分块流式扫描**，块间保留 7 字节重叠，防止跨块的 `moov` 特征漏检，扫描内存恒定约 1MB。
    读 `timescale`（时间刻度）与 `duration`（时长刻度），`时长(秒)=duration ÷ timescale`。兼容 Version 0（32 位）与 Version 1（64 位）；字节序为大端。
  - **mkv / webm**：按 EBML 结构逐元素扫描（`1A45DFA3` 头部 → `1549A966` Info 段），读取 `TimecodeScale`（默认 1e6）与 `Duration`（浮点，毫秒），`时长 = Duration × TimecodeScale ÷ 1e6`。
  - **avi**：RIFF 容器中定位 `hdrl` 下的 `avih` 主头，读 `dwMicroSecPerFrame`（微秒/帧）与 `dwTotalFrames`（总帧数），`时长 = 微秒×帧数 ÷ 1e6`。
  - **wmv / asf**：解析 ASF Header 对象，按 GUID 识别 `File Properties` 对象（Play Duration，100ns 单位），`时长 = PlayDuration ÷ 1e7`。若 Header Size 不足或 File Properties 对象不存在则计为失败。
- 边界：标准容器结构可读；扫不到有效元数据（含结构异常）则计为该文件失败并累计；失败原因文本（`未找到有效moov元数据` 等）。
- 实现：`DurationParser` 静态类：`ParseFile(string)`、`ParseFile(string, out string reason)`、`ReadAll(List<string>, out fail, out failed, threads, …)`（`Parallel.ForEach` 并发，`MaxDegreeOfParallelism` 由调用方传入，支持取消与逐文件进度回调）、`GetVideoFiles`（扩展名过滤）、`ShortReason`（异常截断 200 字符）。

**8. 逐层汇总**
- 功能：树中**每个文件夹**的时长与文件数 = 该文件夹**自身**直接包含视频之和 + **所有子孙文件夹**之和。
- 实现：`VideoScanner.Aggregate` 以**自底向上**顺序遍历（倒序索引），对每个 `FolderItem` 累加自身文件与各子目录已计算的合计，写入 `totals`/`counts` 字典；最后返回根文件夹总时长作为整次统计总长。

**9. 结果反馈**
- 功能：完成后顶栏显示统计结果，并在 `log.txt` 追加记录。
- 内容：顶栏显示 `总时间: X时Y分Z秒`；存在缺失时**顶栏仍只显示总时间**（缺失信息由弹窗汇总，见「结果反馈与缺失提示」）。
- 附加：记录 Info 日志 `文件夹: <路径, 多根用逗号分隔> | 总时间: ... | 耗时: <N> 毫秒`；随后把归总的文件夹结果按层级建立树。

**10. 结果反馈与缺失提示**

各类情况在「总时间栏 / 弹窗 / 日志」三处的处理方式（均已通过自动化测试验证）：

| 情况 | 总时间栏 | 弹窗 | 日志等级 |
|---|---|---|---|
| 文件夹路径无效（单路径） | 显示"文件夹路径无效，请重新输入。" | 不弹 | Warning |
| 部分路径无效（多路径） | 询问是否忽略并继续 | **弹警告** | Warning |
| 重叠路径（多路径） | 询问是否允许重复计时 | **弹信息** | — |
| 正在扫描 | "正在扫描文件夹…" | 不弹 | — |
| 扫描完成（无缺失） | 总时间 | 不弹 | Info |
| 扫描完成（有缺失） | **仅总时间**（不含缺失信息） | **弹警告**汇总缺失项 | Warning（汇总+明细） |
| 扫描已取消 | "扫描已取消。" | 不弹 | Info |
| 查询异常 | — | **弹错误** | Error |
| 图片保存成功 | — | **弹信息**（完成） | Info |
| 图片保存失败 | — | **弹错误** | Error |
| 图片无内容可保存 | — | **弹信息**（提示） | Warning |
| 复制到剪贴板失败 | — | 不弹 | Warning |
| 报表导出成功 | — | **弹信息**（完成） | Info |
| 报表导出失败 | — | **弹错误** | Error |

**缺失明细（文件读取失败 / 目录无法访问 / 超深省略）**
- 弹窗内容：`扫描完成，但存在缺失：\n<N> 个文件读取失败；<M> 个目录无法访问；超过 50 层的目录已省略\n\n详细原因已写入日志文件。`
- 日志明细：每个失败项单独一行 `文件读取失败: <路径>（原因）`、`目录无法访问: <路径>（原因）`、`超过50层目录已省略: <路径>`，每类最多写 200 条，超出追加 `…其余省略，共 N 项`。
- 失败文件原因（`DurationParser.ParseFile` 返回）：文件过小(<16字节) / 未找到有效moov元数据 / moov元数据损坏或不支持 / 其它异常（`异常类型: 消息`，截断 200 字符）。
- 目录失败原因：捕获 `CollectFoldersRecursive` 枚举目录时抛出的异常（如 UnauthorizedAccessException），通过 `DurationParser.ShortReason` 截断记录；**每个失败目录只计数一次**（枚举文件与子目录在同一个 try 内统一计数，避免重复）。

**11. 递归深度上限**
- 功能：最深统计 50 层，防止畸形深层目录导致失控；超过层数的目录记为"超深省略"。
- 实现：`VideoScanner.CollectFoldersRecursive` 以 `depth > MaxDepth(50)` 作为返回条件，并累加 `result.DepthSkipped`、记录 `result.SkippedDirs`。

**11b. 扫描进度条 + 取消**
- 说明：查询过程中在**窗口底部**显示一条自绘进度条（`ProgressBarEx`，进度文字内嵌，结果树在其上方；空闲或扫描结束后隐藏，结果树自动占满），分两个阶段：
  - **收集阶段**：正在枚举目录结构，进度条为不定态滑动动画，内置文字「正在收集目录…」。
  - **解析阶段**：正在读取各文件时长，进度条为确定百分比（`0%–100%`），内置文字 `正在读取 N% (已处理/总数)`（每 100ms 节流一次，避免刷屏）。
- **完成后消失**：扫描成功后进度条先显示 **100% 满格约 0.5 秒**，随后隐藏，结果树向上扩张占满空间。
- **取消按钮**：扫描期间 **[取消]** 按钮可用，点击后向 `CancellationTokenSource` 发送取消；收集与解析两阶段均响应取消（`ThrowIfCancellationRequested` / `ParallelOptions.CancellationToken`），界面显示「正在取消…」后转为「扫描已取消。」并隐藏进度条。
- 实现：`VideoScanner.Run`/`RunMultiple` 通过 `IProgress<ScanProgress>`（`Phase` / `Processed` / `Total`）上报；`Form1` 用自定义 `UiProgress`（`IsHandleCreated` + `BeginInvoke` 封送回 UI 线程）→ `UpdateProgress` 更新控件；`ProgressBar.SetIndeterminate(bool)` 切换滑动/百分比态，`SetProgressVisible(bool)` 负责显隐与结果树高度重算。解析回调对**每个文件**（含失败）都计数并强制上报末条 `N/N`，扫描完成后再同步置满，保证进度条**必然到达 100%**。

### C. 树形结果展示

**12. 树行内容与格式**
- 每行 = `文件夹名 + 两空格 + 总时长 + 两空格 + [视频N]`，例如 `01 基础阶段  153时23分0秒  [视频221]`。
- `N` 为该文件夹（含子级）视频总数；时长 `Format` 输出 `X时Y分Z秒`（总秒数**向下取整到整数秒**后，再拆分为时/分/秒，即 truncate 而非四舍五入）。
- 父子关系按目录层级构建，父行在上、子行依次缩进显示。
- 多根扫描时，根节点按名称排序；若多个根目录同名，节点文字改用**完整路径**显示以免混淆（`BuildTree` 中按 `nameCount` 统计同名根并回退为 `r.FolderPath`）。

**13. 默认展开第 3 层**
- 扫描完成后自动展开到第 3 层（含根目录），便于一眼总览主要分类；更深层级默认折叠，可用树叶下方 `+`/`-` 手动展开收起。
- 实现：`Start_Click` 内遍历根节点调用 `ExpandToDepth`（递归 `node.Expand()`，展开所查询母文件夹及其下两级子文件夹，共3级）。

**13b. 结果筛选（TreeFilter）**
- 说明：对**已构建的结果树**做二次过滤（见「二.6」），过滤条件由「筛选」标签页输入：
  - `FilterOptions`：`Name`（名称关键字，不区分大小写）、`DurationMinHours`/`DurationMaxHours`（时长区间，小时）、`CountMin`/`CountMax`（视频数区间）；任一非空即 `IsActive`。
- 匹配规则：**名称条件对所有节点生效**（含从祖先继承的命中）；**时长/数量条件只作用于叶子文件夹**（`isLeaf`），枝干仅靠名称条件与可见子节点保留（`TreeFilter.Matches`）。使用时长过滤时，视频数为 0 的叶子一律过滤（`MatchesDurationCount`）。
- 应用过程：先完整还原上次过滤（节点、原文本、展开状态），再基于原始树重新过滤——保存展开状态与原始文本到 `FilterState`，`CollectVisibleNodes` 计算可见集，`RecomputeBranchTexts` 把每个可见枝干的文字重算为其下**可见叶子**的累计时长/数量，`FindNonVisibleNodes` 收集并移除不可见节点，最后 `ExpandMatchingAncestors` 展开所有含可见子孙的祖先。
- 合计与显示：`CollectFilteredStats` 递归汇总可见叶子，顶栏显示 `过滤合计: X时Y分Z秒 [视频N]`。
- 高亮：名称命中时，`DrawFilterHighlight` 用 `FindSubstringPositions` 找到关键字所有出现位置，逐段自绘「黑字 + 蓝底白字高亮」，`ExtractName` 先剥离 `  时长  [视频N]` 后缀再匹配。
- 清除：`ClearFilter` 按 `FilterState.Removed` 逆序插回被移除节点，`RestoreOriginalTexts`/`RestoreExpandState` 还原原始文字与展开状态；顶栏恢复 `总时间: …`。
- 输入校验：时长/数量输入非法时弹提示列出无法识别的字段（`Dialogs.Show`），不执行过滤。
- 实现：`FilterState`（`ExpandState`/`OriginalTexts`/`Removed` 三个字典/列表）记录过滤前状态；`TreeFilter.ApplyFilter`/`ClearFilter`/`CollectFilteredStats`/`ExtractName`/`ExtractSeconds`/`ExtractCount` 等静态方法；`Form1` 的 `BtnFilter_Click`/`BtnFilterClear_Click`/`FilterKeyDown`（Enter 应用）。

### D. 行内文字拖选 + 蓝色高亮（自绘）

**14. 拖选文字**
- 操作：在节点文字上按住鼠标左键横向拖动，划过范围即选中部分文字，显示蓝色高亮框；松开后选区保留，可继续操作。
- 行为：只在原行文字上产生选区，不影响点击选中整行的其它行。
- 实现：`MouseDown` 记录 `_anchorChar`；`MouseMove` 中用 `CharIndexAt` 把鼠标 X 换算为字符索引，取 `[start,end)` 后刷新行内。

**15. 拖选阈值（防误触）**
- 微小拖动不会误触发拖选；必须移动超过系统 `DragSize`（通常几个像素）才进入拖选模式。

**16. 空格连续覆盖与吸附**
- 连续覆盖：选区跨越多段空格时，空格本身也一样变蓝（中缝不露白）。
- 吸附：鼠标点落在紧邻空格的字符上时，选区自动拉伸覆盖整串空格，避免选中半个空格/半节。
- 实现：`CharToAt` 对空格 run 回并起始位 + `MouseMove` 内回退补全空格段。

**17. 蓝框视觉细节**
- 无缝：蓝框**紧贴**前文，无多余白缝；
- 不截尾：行尾 `[视频N]` 的 `]` 不被裁掉。
- 实现：将一行拆为「黑字前段 / 蓝底白字高亮段 / 带 `NoClipping` 的后缀段」三段自绘；`TextRenderer.MeasureText` 使用带 `Graphics` 的重载（否则会产生约 +7px 的白缝）；行重绘（`Invalidate`）用整行宽以清除行尾残留蓝底。

### E. 复制功能

**18. Ctrl+C 三级复制**
- 有拖选区 → 只复制选中子串；有选中行 → 复制该行整文本；都没有 → 复制全部可见文本。
- 选区截取前对 `_selStart/_selEnd` 做 `Math.Max(0, Math.Min(…, len))` 钳制到当前文本长度，过滤改写节点文字后仍不越界。
- 实现：`DetailTree_KeyDown` 捕获 `Ctrl+C` → `CopyTreeSelection()` 按优先级走。

**19. 右键上下文菜单「复制」**
- 行为：对某节点点击右键后选择「复制」，则复制**该节点那行**文字；仅当右键行 == 当前选中行时该项才可用。
- 实现：`DetailContextMenu_Opening` 动态设置 `_copyNodeItem.Enabled`。

**20. 右键菜单「复制当前界面文本」**
- 行为：把当前所有**可见（未折叠）**节点文本一次性复制，带层级缩进（每级 4 空格），已折叠的子级不计入。
- 实现：`GetVisibleTreeText` 递归 `AppendNodeTextVisible`，遇到 `!IsExpanded` 即停。

**21. 顶部总时间栏文字可复制**
- 行为：顶部「总时间栏」（`ShowTime`）的文字支持鼠标**拖选局部**、`Ctrl+C` 或**右键 → 复制**整段。
- 实现：`ShowTime` 控件为无边框、只读、底色与窗体一致的 `TextBox`（`ReadOnly=true`、`BorderStyle=None`、`TabStop=false`），可原生选中与复制。

### F. 导出为图片与报表

**21. 保存为图片**
- 行为：右键 → 「保存当前界面文本为图片」→ 弹出保存对话框（PNG 或 BMP）。
- 内容 = 当前可见树文本，渲染为**高清**图片：新宋体 3× 缩放、288dpi、抗锯齿、白底黑字、自动计算宽度与行距；行数超过 30000 或像素预算（`maxPixels=24,000,000`）时按 `budgetScale` 等比缩小，保证位图内存有界。
- 默认文件名 `时间统计_yyyyMMdd_HHmmss.png`；无文本时提示「没有可保存的文本内容」；成功/失败均弹提示。
- 实现：`SaveTreeToImage` + `RenderTextToImage`（先以 scale=1 测量自然宽高，再按像素预算求实际 scale，`Graphics.DrawString` 逐行绘制，`SetResolution(96*scale, 96*scale)`）。

**21b. 导出报表（CSV / HTML）**
- 行为：右键 → 「导出报表…」→ 保存对话框中选择 **CSV** 或 **HTML**（按扩展名/过滤器判断格式）。
- 内容（`ReportExporter`）：
  - **CSV**：首行表头 `文件夹,总时长,时长(秒),视频数`，每文件夹一行（深目录用缩进空格体现层级）；空行后为**失败明细区**：`失败明细` / `（无缺失记录）` 或 `类型,路径,原因` 三列（文件读取失败 / 目录无法访问 / 超过50层目录已省略）。
  - **HTML**：`<!DOCTYPE html>` 页面，含总时间标题行、各文件夹明细表、失败明细表；路径文本经 `HtmlEscape` 转义，单元格支持换行（`white-space:nowrap`）。
  - CSV 字段经 `CsvField` 处理（含逗号/引号/换行时加引号转义）；文件用 **UTF-8 BOM** 写入，Excel 可直接打开不乱码。
- 入口：GUI 右键菜单 `DetailContextMenu_ExportReport_Click`；命令行 `CliRunner` 传 `-o` 时也调用同一导出器。
- 边界：未扫描（无结果）时菜单项置灰；导出失败弹错误并写 Error 日志。
- 区域性：秒数字段 `ToString("0.###", CultureInfo.InvariantCulture)`，跨语言/地区输出稳定（不随系统区域设置变小数分隔符）。

### G. 日志

**22. 日志体系**
- 运行目录下 `log.txt`（UTF-8），每行带 `yyyy-MM-dd HH:mm:ss` 时间戳与等级标签 `[错误]` / `[警告]` / `[信息]`。
- `LogLevel` 枚举：`Off = -1`、`Error = 0`、`Warning = 1`、`Info = 2`；`Log.Append(line, level)` 写日志。
- **输出级别整体由一个设置项控制**：`Settings.Default.LogOutputLevel`（`VideoTime.LogLevel` 类型，`user.config` 持久化），对应设置窗口下拉框四档（全部 / 仅警告及以上 / 仅错误 / 关闭）。判定规则 `IsLevelEnabled(level)`：`cfg == Off` 一律不写；否则 `(int)level <= (int)cfg` 才写（例如 `Warning` 档下 Info 不写、Warning/Error 写）。读取失败时**兜底返回 True** 保证不吞日志。
- 写失败静默忽略（`try/catch` + `lock` 保证线程安全），不影响程序稳定。
- 记录内容：
  - 扫描完成：`文件夹: <路径, 多根用逗号分隔> | 总时间: ... | 耗时: N 毫秒`（Info）
  - 有缺失时汇总：`扫描完成但存在缺失: <汇总> | <路径> | 耗时: N 毫秒`（Warning）
  - 部分路径无效：`部分路径无效: <路径列表>，忽略后继续扫描`（Warning）
  - 缺失明细：`文件读取失败: <路径>（原因）`、`目录无法访问: <路径>（原因）`、`超过50层目录已省略: <路径>`（每类最多 200 条，超出加省略注记，Warning）
  - 路径无效：`文件夹路径无效: <路径>`（Warning）
  - 扫描取消：`扫描已取消: <路径>`（Info）
  - 查询异常：`查询异常: <路径> | <消息>`（Error）
  - 报表导出：成功 `报表已导出: <路径>`（Info）；失败 `导出失败: <消息>`（Error）
  - 剪贴板失败：`复制到剪贴板失败`（Warning）
  - 图片保存：成功 `图片已保存: <路径>`（Info）；失败 `保存失败: <消息>`（Error）；无内容 `保存图片失败: 无可保存的文本内容`（Warning）
  - 命令行：`文件夹: <路径> | 总时间: ...（命令行模式）`（Info）；异常 `查询异常（命令行模式）: <路径> | <消息>`（Error）
- 用途：审计扫描历史、排查失败原因、定位不可访问目录。

### H. 设置记忆与窗口

**23. 设置持久化**
- 关闭窗口时，把「多路径（RootPaths）」「包含子文件夹」「日志输出级别」保存到 `user.config`；下次启动自动回填。
- 实现：`Form1_FormClosing` 把各行非空路径以 `;` 连接写入 `Settings.Default.RootPaths`（并保留 `FolderPath` 兼容旧设置），连同 `IncludeSubfolders` 调用 `.Save()`；`Form1_Load` 读 `RootPaths` 拆分回填各行（历史版本无 `RootPaths` 时回退读 `FolderPath`）。

**23b. 设置窗口**
- 行为：主窗口右上角 **[设置]** 按钮打开模态设置窗口（`SettingsForm`，固定边框、居中于父窗体）。
- 内容：「日志输出级别」下拉框（4 档，见「三.G 22」）；点「确定」把所选等级写入 `Settings.Default.LogOutputLevel` 并 `Save()`、`Log.InvalidateLevelCache()` 使级别缓存失效，点「取消」不保存。
- 实现：`BtnSettings_Click` → `new SettingsForm().ShowDialog(this)`；下拉框用 `LevelItem`（显示文本 + `LogLevel` 值）作条目，加载时回显当前设置值。

**24. 窗口行为**
- 顶部为 `menuStrip1` 菜单（「文件 / 筛选」两个 `ToolStripMenuItem`），点击 `MenuTab_Click` 在 `panelFileTab`（多路径输入）与 `panelFilterTab`（筛选）之间切换显隐并重排上部面板高度。
- 居中显示、标题「时间统计」、默认 420×570；`MinimumSize` 由「窗体边框差 + 内容尺寸」计算。
- 窗口大小随屏幕工作区**自适应并设置上限**，避免超出屏幕。
- 尺寸调整结束后，输入框光标滚动到末尾。
- 实现：`MaximumSize` 限制 + `Form1_ResizeEnd` 中 `ScrollToCaret()`；面板/输入行/进度条/结果树高度统一由 `AdjustUpperPanelHeight()` 计算（`RowTopBase + 行数*RowHeight` 驱动）。

---

## 4. 程序结构

### 4.1 源文件清单

| 文件 | 职责 |
| --- | --- |
| `Form1.cs` | 主窗体逻辑：UI 事件、多行输入、菜单 Tab 切换、树扫描/统计、节点自绘、拖选、复制、导出、日志、筛选、进度条与取消 |
| `Form1.Designer.cs` | 设计器生成的控件声明与布局（含「文件/筛选」两个标签页面板、`ProgressBarEx` 进度条） |
| `Form1.resx` | 窗体资源（图标等） |
| `Program.cs` | 程序入口 `Main`，有参数则走命令行（`CliRunner.TryRun`），否则启动主窗体 |
| `DurationParser.cs` | 多格式时长直读（mp4/mov/m4v/3gp、mkv/webm、avi、wmv/asf），并发解析、扩展名过滤 |
| `VideoScanner.cs` | 扫描引擎：`Run`/`RunMultiple`（收集目录 → 并发解析 → 逐层汇总）、`ScanResult`/`ScanProgress`、`NormalizePath`/`DepthOf`/`Format`、失败明细常量、支持取消与进度回调 |
| `CliRunner.cs` | 命令行入口（`-d/-r/-o/-h`），控制台输出与报表生成，`AttachConsole` 附加父控制台 |
| `ReportExporter.cs` | CSV / HTML 报表生成（含失败明细区，`InvariantCulture` 数值格式） |
| `Log.cs` | 日志体系：`LogLevel` 枚举 + 静态 `Append`（按 `LogOutputLevel` 级别过滤、缓存、`lock` 线程安全） |
| `SettingsForm.cs` / `SettingsForm.Designer.cs` | 设置对话框（日志输出级别下拉） |
| `BufferedTreeView.cs` | 双缓冲 TreeView 自定义控件 |
| `ProgressBarEx.cs` | 自绘进度条：`UserPaint` + 双缓冲，滑动/百分比两态 + 内置 `ProgressText` |
| `TreeFilter.cs` | 结果树过滤：`FilterOptions`/`FilterState`/`TreeFilter`（匹配、可见集、枝干重算、还原、高亮定位、文本解析） |
| `Dialogs.cs` | 自定义模态对话框：图标 + 文本 + 多按钮，自动估算高度、超限滚动 |
| `VideoTime.csproj` | 项目文件（.NET Framework 4.8，WinExe） |
| `VideoTime.sln` | 解决方案文件 |
| `App.ico` | 应用程序图标 |
| `App.config` | 应用配置（4.x 重定向等） |
| `Properties\AssemblyInfo.cs` | 程序集元数据 |
| `Properties\Settings.settings` / `Settings.Designer.cs` | 用户设置定义（多路径 RootPaths、是否含子文件夹、日志输出级别，FolderPath 兼容单路径回填） |
| `Properties\Resources.resx` / `Resources.Designer.cs` | 资源定义 |

### 4.2 主要类与职责

- **`Form1`（主窗体）**
  - 事件处理：`Start_Click`（查询）、`BtnBrowse_Click`（浏览）、`BtnCancel_Click`（取消）、`BtnSettings_Click`（设置）、`MenuTab_Click`（文件/筛选切换）、`BtnAddRow_Click`（加行）、`BtnRemoveRow_Click`（删行）、`InputRow_Leave`（空行回收）、`BtnFilter_Click`/`BtnFilterClear_Click`/`FilterKeyDown`（筛选）、`Form1_Load`/`Form1_FormClosing`/`Form1_ResizeEnd`、`TextBox_Doc_DragEnter`/`DragDrop`
  - 布局：`AdjustUpperPanelHeight`（上部面板/输入行/进度条/结果树高度统一重算）、`ReindexRows`（多行重排）、`LayoutFilterPanel`/`LayoutRangeRow`（筛选面板布局）
  - 树自绘与交互：`DetailTree_DrawNode`、`DrawFilterHighlight`、`DetailTree_MouseDown`/`MouseMove`/`MouseUp`、`DetailTree_KeyDown`、`CharIndexAt`
  - 功能方法：`UpdateProgress`（进度条/百分比）、`SetProgressVisible`（进度条显隐与树高度重算）、`CopyTreeSelection`（钳制越界）、`CopyAllText`、`GetVisibleTreeText`、`SaveTreeToImage`、`DetailContextMenu_ExportReport_Click`、`AppendLog`、`LogFailureDetails`、`BuildTree`、`DetectOverlaps`、`ApplyFilterInternal`/`ClearFilterInternal`
  - 取消支持：`_scanCts`（CancellationTokenSource）贯穿收集与解析，`Form1_FormClosing` 中取消
- **`DurationParser`（静态类，核心解析）**
  - `ParseFile(string)` / `ParseFile(string, out string reason)`：多格式时长直读；`ReadAll(List<string>, out fail, out failed, threads, …)`：并发批量读取（可取消、可逐文件回调）；`GetVideoFiles`：按扩展名白名单过滤；`ShortReason`：异常截断。
- **`VideoScanner`（静态类，扫描引擎）**
  - `Run(root, recursive, ct, progress)`：单根：`collect`（收集目录）→ `parse`（并发解析，上报 `ScanProgress`）→ `Aggregate` 自底向上汇总；`RunMultiple(roots, …)`：多根并行收集/解析后合并。
  - `ScanResult` 产出：`TotalSeconds` / `TotalFileCount` / `FolderResults` / `FailedFiles` / `FailedDirs` / `SkippedDirs` / `FailCount` / `DirFail` / `DepthSkipped` / `FolderSet`。
  - 共享工具：`NormalizePath`（`/`→`\`、去引号空白）、`DepthOf`（基于 `FolderSet` 的 ownerRoot 归属算法）、`Format`（时/分/秒）、`DepthSkippedLabel`、`LabelFileFailed`/`LabelDirFailed`。
- **`TreeFilter`（静态类，结果过滤）**
  - `Matches`（匹配规则）、`ApplyFilter`/`ClearFilter`（应用/还原，`FilterState` 记录展开状态、原始文本、被移除节点）、`CollectFilteredStats`（过滤合计）、`FindSubstringPositions`（高亮定位）、`ExtractName`/`ExtractSeconds`/`ExtractCount`（节点文本解析）。
- **`CliRunner`（静态类，命令行）**
  - `TryRun(args, out exitCode)`：解析 `-d/-r/-o/-h`；`AttachConsoleForOutput` 附加父进程控制台；目录不存在/未知参数 → 退出码 1；输出 `总时间/已统计视频/缺失`，`-o` 时调用 `ReportExporter`。
- **`ReportExporter`（静态类）**
  - `Export(path, result, format)` → `BuildCsv` / `BuildHtml`，均含失败明细区；UTF-8 BOM 写出；秒数用 `InvariantCulture` 格式化。
- **`Log`（静态类）**
  - `Append(line, level)`：按 `Settings.Default.LogOutputLevel` 过滤（带缓存）、`lock` 写 `log.txt`；`InvalidateLevelCache` 在设置保存后失效缓存。
- **`SettingsForm`（设置对话框）**
  - 日志输出级别下拉（`LevelItem`），确定时写 `LogOutputLevel`、`Save()` 并失效级别缓存。
- **`ProgressBarEx : ProgressBar`**
  - 自绘进度条：`UserPaint`+`OptimizedDoubleBuffer`，`OnPaint` 画轨道/填充（`ProgressBarRenderer`）与靠左 `ProgressText`；`SetIndeterminate(bool)` 用 33ms `Timer` 滑动动画或按 `Value/Maximum` 画填充。
- **`BufferedTreeView : TreeView`**
  - 自定义 OwnerDraw 双缓冲动画，配合拖选，避免闪烁；并在 `WM_PAINT` 双缓冲时增加 `margin=16` 像素缓冲避免行尾 `]` 被裁剪。
- **`Dialogs`（静态类）**
  - `Show(title, message, icon, buttons…)`：自绘模态对话框（图标 + 换行文本 + 多按钮），`TextRenderer.MeasureText` 估算高度，超出屏幕则 `AutoScroll` 滚动。
- **`Program`**
  - 线程入口，`[STAThread]`；`args.Length > 0` 时走 `CliRunner.TryRun`（成功即 `Environment.Exit`），否则启动主窗体；注册全局异常处理器（写 Error 日志 + 弹窗）。

### 4.3 数据流（一次查询的完整流程）

```
用户点「查询」（多行输入合并为 roots 数组）/ 命令行 -d 目录
      │
      ▼
VideoScanner.RunMultiple ──► 每根 CollectFoldersRecursive 收集「文件夹→文件列表」层级
      │                          （进度：不定态，多根并行收集）
      ▼
parse：每根 DurationParser.ReadAll（多线程，可取消，逐文件进度，0-100%）
      │                          外层 root 与内层文件并发度收敛，避免 threads²
      ▼
各根 Aggregate 自底向上累加 → merged 合并各根结果（时长/失败/超深）
      │
      ▼
Form1：Format 格式化 + BuildTree 构建 TreeNode（名 + 时长 + [视频N]），展开到第 3 层
      │
      ▼
界面显示总时长 + 写日志 log.txt；「筛选」标签页可对结果树二次过滤并高亮；右键可导出 CSV/HTML 报表
```

### 4.4 绘制/选中框结构

自绘节点文字按「前段/高亮段/后缀段」三段绘制：

```
（前段，黑字白底）  （高亮段，蓝底白字）  （后缀段，黑字 NoClipping）
文件夹名            12时34分56秒          [视频221]
 |<----preW---->|<---hlRight 选区宽--->|
```

---

## 5. 关键技术实现要点

- **多格式视频时长免解码解析**：`DurationParser` 按格式族分别解析——mp4/mov/m4v/3gp 查 `moov→mvhd` 读 `timescale`/`duration`（自适应扫描窗口：尾部 4MB → 头部 4MB → 尾部 128MB，每窗口按 1MB 分块流式扫描，内存恒定 ~1MB）；mkv/webm 按 EBML 元素读 `Info→Duration×TimecodeScale`（double 用 `BitConverter.Int64BitsToDouble` 零分配转换）；avi 读 `hdrl→avih` 的 `dwMicroSecPerFrame`（偏移 +8）与 `dwTotalFrames`（偏移 +24）按标准 RIFF 布局解析；wmv/asf 读 File Properties 的 Play Duration。全部手工字节解析，兼容 32/64 位头与大/小端。
- **多线程**：`DurationParser.ReadAll` 用 `Parallel.ForEach` 并行读文件，线程数随处理器核数自适应；结果写入**按 files 顺序排列的定长数组**（`ordered[]` + `failFlags[]`，`Interlocked` 标记失败），最终按顺序组装 `Dictionary`，避免并发字典无序与临时分配。
- **多根并行**：`RunMultiple` 外层按根 `Parallel.For`、内层 `ReadAll` 双并行，外层 `outerThreads = min(roots.Length, max(2, threads/2))`、内层 `innerThreads = max(2, threads/outerThreads)`，把并发度收敛到 ~线程数而不是乘积；进度按全部分根文件总数统一计数（`fileDone` 对每个文件计数 + 100ms 节流 + 强制末条 `N/N`）。
- **可取消**：`CancellationTokenSource` 贯穿目录收集（`ThrowIfCancellationRequested`）与并发解析（`ParallelOptions.CancellationToken`）；窗口关闭或点「取消」即中止，取消时顶栏显示「扫描已取消。」并写 Info 日志。
- **进度上报**：`IProgress<ScanProgress>`（collect/parse 两阶段，`Phase/Processed/Total`），进度事件节流 100ms；`Form1` 用自定义 `UiProgress`（`IProgress<ScanProgress>`，内部 `IsHandleCreated` 检查 + `BeginInvoke` 封送回 UI 线程）。解析回调对**每个文件**都计数并强制上报末条 `N/N`，扫描完成后再同步置满，保证进度条**必然到达 100%**；完成后满格约 0.5 秒即隐藏底部进度条，结果树随之扩张占满。
- **自绘进度条**：`ProgressBarEx` 继承 `ProgressBar`，`SetStyle(UserPaint | AllPaintingInWmPaint | OptimizedDoubleBuffer | ResizeRedraw)`；`OnPaint` 先 `ProgressBarRenderer.DrawHorizontalBar` 画轨道、再画填充（不定态时 33ms `Timer` 滑动高亮块，确定态按 `Value/Maximum` 画），最后 `TextRenderer.DrawText` 靠左画 `ProgressText`。比「原生进度条 + 透明 Label」方案更稳（透明 Label 只显示父控件底色、无法叠加原生填充）。
- **路径归一化**：`VideoScanner.NormalizePath` 统一 `Trim().Trim('"')` 并把 `/` 归一化为 `\`，使正斜杠/反斜杠路径等价可混用（GUI、CLI、重叠检测、`DepthOf` 全部复用）。
- **重叠检测**：`DetectOverlaps` 两两比对（忽略大小写）：`相等` 判重复路径，`a.StartsWith(b + "\\")` 双向判嵌套包含；存在重叠时弹提示由用户决定是否允许重复计时。
- **结果过滤**：`TreeFilter` 在已构建树上原地操作——`FilterState` 保存展开状态与原始文本，`ApplyFilter` 计算可见集后重算枝干文字、摘除不可见节点并展开祖先；`ClearFilter` 按记录逆序插回并还原文字/展开状态。名称条件对所有节点生效、时长/数量只作用于叶子，保证「枝干累计 = 过滤后可见叶子的合计」自洽。
- **日志体系**：`LogLevel`（Off/Error/Warning/Info）+ 单一下拉设置 `LogOutputLevel` 控制输出范围 + `lock` 线程安全写 `log.txt`（UTF-8）；读取级别带**缓存**（`SettingsForm` 保存后 `Log.InvalidateLevelCache()` 失效），避免每次写日志读配置；失败原因由 `ShortReason` 截断到 200 字符。
- **报表导出**：`ReportExporter` 生成 CSV（字段转义用 `static readonly` 特殊字符数组 + UTF-8 BOM，Excel 可直接打开）与 HTML（转义 + 表格样式），均含失败明细区；GUI 右键与命令行 `-o` 共用。目录层级深度统一复用 `VideoScanner.DepthOf`；秒数用 `CultureInfo.InvariantCulture` 格式化，跨区域稳定。
- **命令行**：`CliRunner.TryRun` 解析 `-d/-r/-o/-h`；`AttachConsole(-1)` 附加父控制台后用 `Console.SetOut/SetError` 输出；退出码约定 0=成功、1=参数/目录错误。
- **OwnerDraw + 双缓冲**：自绘节点，配合 `WM_PAINT` 内存位图 + `margin` 缓冲消闪烁、防截断；`DrawNode` 绘制体包 try/catch 降级为默认绘制，避免自绘异常拖垮 UI。
- **坐标命中**：`CharIndexAt` 用 `TextRenderer.MeasureText`（带 `Graphics`）**单字符宽度累加 + 宽度缓存数组**定位字符（O(n) 而非 O(n²)），包含对空格 run 的吸附处理。
- **自绘对话框**：`Dialogs.Show` 用 `TextRenderer.MeasureText`（`WordBreak` 换行）估算文本高度，配合按钮行计算对话框高度；超过屏幕工作区时 `AutoScroll` 滚动，支持任意数量的自定义按钮。
- **全局异常兜底**：`Program.cs` 注册 `Application.ThreadException`（UI 线程）与 `AppDomain.UnhandledException`（非 UI 线程），统一写 Error 日志并弹出友好提示。

---

## 6. 构建方式与环境要求

### 运行环境
- **64 位 / 32 位 Windows**（均可）。
- 系统已安装 **.NET Framework 4.8**（Win10/11 基本默认包含）。

### 构建（开发者）
安装有 **Visual Studio 2017+** 或 **MSBuild 15+** 时，直接：

```
MSBuild VideoTime.csproj /t:Rebuild /p:Configuration=Debug /p:Platform=AnyCPU
```

也可用 `dotnet SDK` 附带的 **Roslyn csc** 手动编译，目标 .NET Framework 4.8（引用 `Reference Assemblies\…\v4.8`）：

```
dotnet "C:\Program Files\dotnet\sdk\9.0.304\Roslyn\bincore\csc.dll" ^
  /target:winexe /platform:anycpu /nostdlib /out:bin\Debug\VideoTime.exe ^
  /r:…\v4.8\mscorlib.dll /r:…\v4.8\System.dll /r:…\v4.8\System.Windows.Forms.dll … ^
  Form1.cs Form1.Designer.cs Program.cs ^
  Properties\AssemblyInfo.cs Properties\Resources.Designer.cs Properties\Settings.Designer.cs
```

**产物**：`bin\Debug\VideoTime.exe`（含内嵌资源）。

**回归测试**：仓库 `tests\` 内含六份测试脚本 + 三个 GUI 回归探针，共 **245 项回归断言 + 20 项探针断言**，一键运行：

```
powershell -ExecutionPolicy Bypass -File tests\run-tests.ps1
```

- 测试脚本（`run-tests.ps1` 统一编排）：`test_parsers.ps1`（解析器单测）、`test_robust.ps1`（鲁棒性边界）、`test_cli.ps1`（CLI 与导出端到端）、`test_cancel.ps1`（取消机制）、`test_multiroot.ps1`（多根聚合 + 过滤，调用 `MultiRootFilterTest.cs` 编译出的辅助程序）、`test_settings2.ps1`（日志级别配置）。
- 脚本会自动定位 MSBuild（`lib.ps1` 用 `vswhere` 探测）、编译 Debug 产物、用 Roslyn csc 编译测试辅助程序 `CollectProgress.dll`（内含进度事件收集 `CollectProgress` 与取消注入 `CancelRecorder`），再运行全部测试；`test_settings2.ps1` 会备份/恢复用户 `user.config`（若配置尚不存在则先创建最小模板）。测试脚本需以 UTF-8（带 BOM）保存。
- GUI 回归探针（`run_probes.ps1`，逐一编译成独立 exe 运行）：`ProbeCancelRace`（取消竞态）、`ProbeStaleSelection`（选区钳制越界）、`ProbeCsvInvariant`（CSV 导出区域性不变式）。

---

## 7. 常见问题 FAQ

**Q1：点「查询」没反应？**
A：确认至少一个输入行是存在的文件夹路径；若该路径下没有视频或子目录，统计结果为空属正常。多路径时若某行无效，会弹窗询问是否忽略后继续。

**Q2：统计的时长是整份视频还是只统计某个视频？**
A：是**累计**时长——每个文件夹行的时长 = 该文件夹及其所有子文件夹中全部视频的时长之和。

**Q3：实数太大了能算吗？**
A：秒级统计上千文件；但受 `MaxDepth=50` 深度限制。

**Q4：为什么有些文件显示失败？**
A：个别视频格式特殊（无标准时长元数据结构，如损坏的 mp4）或文件损坏，会被记为"读取失败"。扫描结束时会**弹出警告窗**汇总缺失（文件读取失败 / 目录无法访问 / 超深省略），总时间栏只显示总时间；每个失败项的**路径与原因**都写入 `log.txt`。

**Q5：日志和设置存在哪？**
A：`log.txt` 在 `VideoTime.exe` 所在目录（按设置窗口里的「日志输出级别」过滤）；设置在 `user.config`（`C:\Users\<用户>\AppData\Local\VideoTime` 下类似路径）。

**Q6：能导出 Excel/PDF 吗？**
A：支持**导出报表**：结果树上右键 → 「导出报表…」可存为 **CSV**（Excel/WPS 可直接打开）或 **HTML**（浏览器查看），均含失败明细。命令行下用 `-o` 也能生成。此外仍支持 `Ctrl+C`/右键复制为文本、以及右键保存为 PNG/BMP 图片；暂不支持直接导出 PDF。

**Q7：能同时统计多个文件夹吗？**
A：可以。在「文件」标签页点右上角 `[+]` 添加多个输入行，每行填一个文件夹（或浏览/拖放），一次查询会并行统计并合并到同一棵树。多路径重叠（重复 / 嵌套包含）时程序会提示，可选择是否允许重复计时。

**Q8：过滤后怎么看原始结果？**
A：「筛选」标签页点 `[清除]` 即还原全部节点、原始文字与展开状态，顶栏恢复总时间；多次过滤前会先完整还原，再基于原始树重新过滤。

---

## 8. 未来规划与候选功能

> 本节基于现有功能（目录扫描 → MP4 时长直读 → 树形展示 → 拖选复制 → 存图 → 日志）提出未来可完善或开发的方向，分为「大功能」与「小功能改进」两类，并标注优先级供取舍。

### 8.1 候选大功能

| # | 功能 | 说明 | 复用基础 | 优先级 |
| --- | --- | --- | --- | --- |
| 1 | 学习进度追踪 | 记录历史扫描与"累计已学习时长"，跨周/月统计曲线 | 现有 `log.txt` 历史 | 高 |
| 2 | 视频属性信息面板 | 选中节点显示分辨率/编码/码率/大小/时长等元数据 | 解析扩展 | 中 |
| 3 | 重复文件检测 | 检测同名/同大小/哈希相同的视频并支持一键去重 | 已有去重脚本 | 中 |
| 4 | 大数据量 + 渐进加载 | 万级节点异步分段加载、滚动加载，避免卡顿 | `ExpandToDepth` | 低 |
| 5 | 多语言 / 主题切换 | 界面多语、深色主题 | UI 层 | 低 |

> 已具备：多视频格式支持（mp4/mov/m4v/3gp、mkv/webm、avi、wmv/asf）、数据报表导出（CSV/HTML）、命令行/批量模式（`-d/-r/-o`）、扫描进度条 + 取消按钮、**多根目录聚合**（多行输入 + 重叠检测 + 并行合并）、**树内过滤**（按名称/时长/数量过滤并高亮命中）。

### 8.2 小功能改进

**统计与展示**
- 节点排序（按时长 / 文件数 / 名称）
- 时长单位与精度可切换（秒 / 分钟 / 小时）
- 单击选中文件夹即显示该行明细（文件数、平均时长）
- `[视频N]` 数字千分位；父行加粗 / 色块区分层级
- 右键「展开全部 / 折叠全部」与快捷键
- 复制快捷键补充：`Ctrl+Shift+C` 复制带层级文本、`Ctrl+Ins` 复制时长

**交互增强**
- 拖选体验：双击选词、`Shift+点击` 区间选择、`Home`/`End` 全选、拖选时实时显示已选字数
- 右键「复制时长」「复制路径」
- 窗口最大化 / 高 DPI 高清缩放适配

**健壮性**
- 失败文件列表可查看 / 导出 / 定位
- 刷新当前目录快捷键（F5）；界面内查看日志
- 设置持久化窗口位置与尺寸
- 占用 / 权限失败的分级提示与重试
- 记忆上次展开状态

**资源与元数据**
- 根路径显示 / 复制按钮；每行 tooltip 显示完整路径
- 计算并显示总占磁盘体积（GB）

---

## 9. 代码质量与已知权衡

### 9.1 健壮性与效率设计

**健壮性**
- 全局异常兜底：`Program.cs` 注册 `Application.ThreadException` + `AppDomain.UnhandledException`，任何未处理异常写 Error 日志并弹友好提示。
- `DetailTree_DrawNode` 绘制体整体 try/catch：自绘异常时降级为默认绘制，防止一次异常让整棵树的绘制连续报错。
- 进度回调用自定义 `UiProgress : IProgress<ScanProgress>`：`Report` 内先判 `IsHandleCreated`/`IsDisposed` 再 `BeginInvoke`，窗体关闭后不会向已销毁句柄回调。
- `BufferedTreeView` 的 `WM_PAINT` 双缓冲：`CreateCompatibleDC/Bitmap` 全部包在 try/finally 中释放（`SelectObject` 恢复旧位图 → `DeleteDC` → `DeleteObject`），DC/位图创建失败时不执行 SelectObject。
- 复制选区截取前对索引钳制到当前文本长度；`Form1_FormClosing` 遍历输入行用 `_inputRows.ToArray()`，避免迭代中移除元素。

**效率**
- `CharIndexAt`：O(n) 单字符宽度累加 + 缓存数组定位（不用每次 `Substring` 重测）。
- `ParseMp4`：三窗口扫描「尾部 4MB → 头部 4MB → 尾部 128MB」，每窗口按 **1MB 分块流式读取**（保留 7 字节重叠避免跨块漏检），单文件扫描内存恒定约 1MB、与文件大小无关。
- EBML float/double：使用 `BitConverter.Int64BitsToDouble` 与 `[StructLayout(Explicit)] FloatBits` 联合体，栈上零分配、无临时数组。
- `CsvField`：特殊字符数组为 `static readonly`，不随调用新建。
- `FileStream` 顺序读 buffer 64KB（大文件解析更少系统调用）。
- `Log.IsLevelEnabled`：缓存 `LogOutputLevel`，`SettingsForm` 保存后 `Log.InvalidateLevelCache()` 失效，避免每次写日志读配置。
- 多根扫描并发度收敛：外层 root 与内层文件读取并发相乘，`outerThreads`/`innerThreads` 按根数与核数动态分配，避免 `threads²` 过度并发。

**结构与复用**
- `NormalizePath` / `DepthOf` / `Format` / 失败明细文案（`文件读取失败` / `目录无法访问` / `DepthSkippedLabel`）统一收敛在 `VideoScanner`，GUI、CLI、报表导出共用。
- 进度 100% 保证：`ReadAll` 按 `files` 顺序写入定长数组 `ordered[]/failFlags[]`，`fileDone` 对**每个**文件（含失败）都计数并强制上报末条 `N/N`，扫描完成后再同步置满，进度条必然走到 100%。
- 进度条布局（树在上、进度在下）：进度条位于窗口底部，空闲与扫描结束后隐藏，`SetProgressVisible(bool)` 按 `ClientSize.Height` 重算 `DetailTree` 高度使其自动占满（成功后先显示 100% 约 0.5 秒再隐藏）。

### 9.2 已知权衡与边界

- **`ThreadException` 处理器中弹窗可能被 `MessageBox.Show` 再触发异常**：已用双层 try/catch 兜底，最坏情况仅丢失弹窗、日志仍落盘。
- **命令行控制台编码**：CLI 直接输出依赖系统代码页（中文 Windows 为 GBK/936）。测试脚本用 `ProcessStartInfo.StandardOutputEncoding=936` 读取；如需跨系统稳定可用 `Console.OutputEncoding=UTF8` 强制（但中文控制台可能显示乱码，故默认不强制）。
- **`user.config` 为 GUI 与 CLI 共用**：日志级别设置对二者同时生效；`test_settings2.ps1` 通过备份/恢复保证测试不污染真实配置。
- **进度条文字与进度条重叠且都可见**：自绘控件 `ProgressBarEx`（`UserPaint`+`OptimizedDoubleBuffer`），`OnPaint` 先画轨道/填充、再靠左画 `ProgressText`；不确定阶段用 33ms `Timer` 滑动高亮，确定阶段按 `Value/Maximum` 画填充。控件仅 360×20px，重绘开销微秒级，不影响扫描（扫描在 `Task.Run` 后台线程）。
- **EBML 4 字节 float 罕见**：MKV 绝大多数用 8 字节 double，float 走 `FloatBits` 联合体仍为栈上零分配，无需 unsafe。
- **`CsvField`/`HtmlEscape` 依赖路径字符串本身为合法文本**：若路径含异常控制字符，HTML 输出未做 `\u0000` 过滤（现实几乎不出现，暂不处理）。
- **多进程并发写 `log.txt`**：`lock` 仅保护进程内；两个 VideoTime 实例同时写可能有交错行（低概率，未加跨进程文件锁）。
- **多根重叠计时**：重叠路径是否允许重复计时由用户确认；若允许，嵌套目录的时长会被计入多个根，报表/日志为原始累计值。
- **超大结果树导出图片**：超过 30000 行或像素预算时按比例降缩放而非截断，保证位图内存有界、内容完整。

---

*文档完。*