# VideoTime（视频时长统计）

> 一款 Windows 桌面小工具，帮你一键统计电脑中各个文件夹里的视频总时长，看看"时间都花在了哪里"。

VideoTime 无需逐个打开视频，扫描后即可在几秒内汇总出每个文件夹的视频总时长，并用清晰的树形列表呈现。

---

## 主要功能

- **自动统计视频时长**：选择任意文件夹，即可统计其中（可选含所有子目录）视频的总时长与数量。
- **树形展示结果**：按文件夹层级展示，每一项清楚标注 `文件夹名 / 总时长 / 视频数`，一眼看清各目录的占比。
- **累计汇总**：每个文件夹的时长 = 它直接包含的视频 + 所有子文件夹视频之和，方便看出哪类内容耗时最多。
- **复制与分享**：支持复制选中行、复制当前可见的全部文本，也可一键保存为图片（PNG / BMP）用于发圈或写报告。
- **多种选目录方式**：支持直接输入路径、点选浏览、或直接把文件夹拖进窗口。
- **记住你的选择**：上次选择的目录与选项会自动保存，下次打开即可直接使用。

---

## 快速上手

1. **选择目录**：输入路径、点 [浏览] 选择，或直接拖一个文件夹到窗口内。
2. **决定范围**：勾选"包含子文件夹"，可统计所选目录下的所有层级；不勾选则只统计该目录直属的视频。
3. **点击 [查询]**：静候片刻，树形列表便会出现统计结果。

> 目录较大时，界面顶部会显示扫描进度，顶窗也会给出总时长结果。

---

## 结果怎么看

每条结果显示为：

```
课程视频  1234时56分34秒  [视频401]
  ├ 01 基础阶段  153时23分0秒  [视频221]
  └ 02 提高阶段   89时11分12秒  [视频177]
```

- **时长**：该文件夹含其所有子文件夹的**累计总时长**。
- **[视频N]**：该文件夹（含子文件夹）内的视频数量。
- 列表默认展开前 2 层，更深的层级可点击 `+` / `-` 手动展开或收起。

---

## 复制与导出

- **复制选中项**：选中某一行后按 `Ctrl+C`，或右键 → 复制，即可复制该节点。
- **复制当前显示的全部文本**：右键 → 复制当前可见文本，可把可见结果一次性复制出来。
- **保存为图片**：右键 → 保存当前文本为图片，可保存为 PNG 或 BMP，适合分享。

---

## 常见问题 (FAQ)

**点 [查询] 没有反应？**
请确认输入的是正确存在的文件夹路径；若开启了"包含子文件夹"，也请确认目标文件夹内确含视频。

**统计的时长和单个视频对不上？**
多数文件夹显示的是**累计时长**（含子文件夹），并非单文件时长。

**为什么有些视频提示读取失败？**
个别文件格式异常或文件损坏时无法读取，这类文件不会被计入，并在顶部提示失败数量。

**能导出到 Excel / PDF 吗？**
目前支持复制为文本和保存为图片。如需导出表格或更多格式，可关注后续版本更新。

---

## 系统要求

- **操作系统**：64 位或 32 位 Windows。
- **运行环境**：安装有 **.NET Framework 4.8**（Windows 10 / 11 通常已自带）。

---

## 更新日志 / 规划

新功能的开发与扩展将记录于此，敬请期待更多支持的视频格式、导出格式与增强功能。

---

感谢使用 VideoTime。希望你面对"看过多少视频"的同时，也能更从容地分配自己看过的时光。